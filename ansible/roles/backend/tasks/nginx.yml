# Backend Nginx Reverse Proxy Configuration

- name: Enable Amazon Linux Extras nginx repository
  become: true
  command: amazon-linux-extras enable nginx1
  changed_when: false

- name: Install Nginx
  become: true
  yum:
    name: nginx
    state: present
    enablerepo: amzn2extra-nginx1

- name: Ensure Nginx configuration directory exists
  become: true
  file:
    path: /etc/nginx/conf.d
    state: directory
    mode: '0755'

- name: Deploy Nginx reverse proxy config for backend
  become: true
  template:
    src: backend-nginx.conf.j2
    dest: /etc/nginx/conf.d/backend.conf
    mode: '0644'
  notify: restart nginx

- name: Ensure Nginx is enabled and started
  become: true
  service:
    name: nginx
    state: started
    enabled: yes

- name: Check Nginx configuration is valid
  become: true
  command: nginx -t
  changed_when: false
